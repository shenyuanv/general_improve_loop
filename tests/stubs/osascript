#!/usr/bin/env bash
# tests/stubs/osascript — capture macOS notifications instead of showing them.
set -u
CAP="${T_CAP:-$HOME/.captures}"; mkdir -p "$CAP"
printf 'osascript %s\n' "$*" >>"$CAP/notifications.log"
exit "${T_OSA_RC:-0}"

# general_improve_loop daily — 2026-07-04   (run 20260704-000614; duration/cost in runs.jsonl)
Status: 🟡 ATTENTION — gates green (48/48, xfail=0, doc-contract lint green on night one), live current at 8c705fe, floors quiet, funnel 5 open / 0 orphans / 0 decisions; one leisure Needs-you (close drill-residue #49 so a real CI failure can't dedup onto it). First `--scheduled` relay fires 03:33 today — Focus #2's Option A meter starts tonight.
> NOTIFY: deployed — owner promoted live 0cc1362 → 42a5cf0 → 2c6bd77 → 8c705fe (3 manual promotes 12:04–15:55Z, all verified healthy; auto lane again unexercised)

## Needs you
- [ ] **Close issue #49 — nightly-drills drill residue (action:operator, leisure).** WHY blocked: only humans close issues (write-policy: loop closure happens via `Fixes #n` merges only), and #49 has nothing to fix — it was filed by the CI filer from YOUR planted `workflow_dispatch` drill at 16:05Z (the failing step is literally "plant failure (filing-path test only)"; classify + file steps green — 8c705fe's lane-aware routing worked end-to-end). ACTION: close https://github.com/shenyuanv/general_improve_loop/issues/49 as drill-verified, optionally after one clean `gh workflow run nightly-drills`. ONCE DONE: a future REAL nightly-drills failure files a fresh issue instead of deduping onto a drill-titled one and hiding behind it.

## Guardian (safety)
- 🟢 CI watchdog: the 16:05Z nightly-drills red is the owner's planted filing-path drill — filer + lane routing PROVEN at 8c705fe (only failing step = "plant failure (filing-path test only)"; classify + file steps green ⇒ #49, correct labels `bug/loop-filed/action:operator/component:ci`); weekly-macos green; zero red-without-issue. Close #49 — see Needs you.
- 🟢 Security signals: 0 auth failures; identity pin live (shenyuanv active via GH_TOKEN, saster985 present-inactive, 0 flips since 07-03 00:45Z); 3 errors/24 h all environmental quota-429, 0 code-caused, none scheduled; history-gated anomaly rules correctly skipped (<1 wk history).
- 🟢 Floors quiet + proven: 0 no-go reverts / 0 self-accept strips / 0 breaker trips; no PAUSE/DEMOTED/BLOCKED; gates 48/48; rollback.sh executed twice for real on 07-03 (drill #8 + L4 tamper leg), both restored green — recovery no longer theoretical.
- 🟢 Standing decisions honored, premises verified live: #5 GIT_REMOTE gone (0 refs), #20 identity pin active in config, #47 Option A fully enacted (DIRECTION Focus #2 reworded + all 6 launchd plists carry `--scheduled` into the live checkout). #47's premise-break trigger (a scheduled run failing on quota) is armed, not yet testable — first scheduled relay 03:33 today.
- 🟡 Durability: deploys.jsonl lagged promotions.jsonl by 3 owner promotes (12:04/14:07/15:55Z incl. live 8c705fe) — open #44's premise compounding (4th consecutive backfill; cooling ends 11:49Z today). Backfilled this run (rows committed below), not re-filed.
- 🟡 Validation gap: live 8c705fe is ~13 promotes past the last journey-validated engine (PASS@6eb6b6a, 07-03 01:23Z); every promote passed the full post-promotion gate; today's 04:33 journey closes the gap — escalate only on a FAIL there.

## Operator (deploys · prod health · queue funnel)
**Deploys.** Live checkout CURRENT at `8c705fe` (drift.sh exit 0; == main == origin) — no deploy attempted, no rollback, no `ops/BLOCKED`. The auto lane's first real exercise again didn't materialize: the owner manually promoted all three of yesterday evening's commits (12:04Z → 42a5cf0, 14:07Z → 2c6bd77, 15:55Z → 8c705fe, per promotions.jsonl) before this run. 3 backfill rows appended to `ops/ledger/deploys.jsonl` this run (open #44 tracks mechanizing this — 4th consecutive hand-backfill). 24 h promotion ledger: 20 rows (1 first-promote, 17 promote, 2 rollback — both 07-03 drills, not incidents).

**Prod health (the loop is prod).** 26 runs / 24 h: 22 success, 3 error (all environmental quota-429, all pre-dating tonight; in-repo trace gap = open #35), 1 paused drill. Cost $210.38 (orch 90.55 / fixer 62.75 / verifier 39.81 / e2e 12.26 / manager 5.01); 36 commits; 0 timeouts; 0 no-go reverts (Focus #1 holding). Journey identities excluded from harvest: 2. Focus #2: `scheduled_success_rate` null by construction — every run so far is manual; 6/6 launchd jobs loaded, the ≥95% scheduled meter starts accumulating at 03:33 today.

**Queue funnel** (mechanical, `bin/funnel.sh` — exit 0, annotated):

| item | state | next actor | next action |
|---|---|---|---|
| #49 [nightly-drills] failed on a clean runner (planted drill) | operator task | owner closes (drill residue — Needs you) | owner leisure |
| #35 errored non-orchestrator runs leave no in-repo trace | cooling | fixer dispatcher | after 2026-07-04T09:13:04Z |
| #40 fixer dispatcher must pre-assign new-test ids | cooling | fixer dispatcher | after 2026-07-04T09:52:10Z |
| #41 --check-nogo drill doesn't exercise the remote-tip scan | cooling | fixer dispatcher | after 2026-07-04T09:53:28Z |
| #44 owner-manual promotes leave no in-repo deploy trace | cooling | fixer dispatcher | after 2026-07-04T11:49:38Z |

funnel: open_issues=5 loop_prs=0 orphans=0

Annotations: all four fix-lane items cool past today's 05:03 fixer (expiries 17:13–19:49 local) — **that run is an expected no-op**; they thaw for the 07-05 05:03 fixer, which then faces ~8 eligible vs MAX_FIXES_PER_NIGHT=3 (standing backlog watch re-arms). #40/#41 lack `loop-filed` but are owner-authored in loop filing format — not user reports, no NOTIFY; they are human-filed, so outside grooming scope. Open 🙋: none. Open loop-pr PRs: none.

**Closed in 24 h (traces):** #47 🙋 decided **Option A** (scheduled-runs basis for Focus #2), enacted in 2c6bd77 + DIRECTION edit · #48 design brief enacted as the doc-contract lint in 2c6bd77 · #29 #36 #45 #46 doc-drift set closed by the same enactment commit · #42 #43 nightly-drills noise closed ~10:56Z, superseded by 8c705fe hardening · #25 #30 #34 via merged fix PRs #39 #37 #38.

## Analysis (E2E trends)
- Journeys 14 d: PARTIAL→PASS (▁█, 2 rows; a 3rd attempt was quota-killed leaving no row = open #35, corroborated not re-filed); no stage has failed twice; FLOORS stages 2/2 PASS.
- Latency: 58→57 s install→first-green (median 57.5, 0.99× — stable).
- Liveness: journey 0.63 d ✅ but validates 6eb6b6a — live 8c705fe is ~13 promotes ahead; today's 04:33 journey is the gap-closer (no journeys.jsonl row by the evening pass ⇒ e2e-loop liveness FAIL, file then). Tester 0 rows ON PLAN (first sweep Sun 07-05); 0 loop-PRs, 0 stuck accepted bugs.
- Environment: all 3 window errors = quota-429, environmental, pre-#37 classification; no auth-path or infra_flags streaks; standing escalation trigger unchanged (a scheduled-run 429 or 2 breaker strikes).
- Cost: orch 1.32× · fixer 1.38× · verifier 1.31× (monotonic 4.44→8.77 tracks merge volume — expect a fall on tonight's empty PR queue) · e2e 1.0× — all <2× own median, Focus #4 green; window $210.38/26 runs; orchestrator §3.5 check independently clean (latest $15.83 vs median $12.00, nothing >$25).
- Diff accounting: 26/26 reconcile — newest two verified byte-exact (dfe0362 +410/−290; manager 87066bf +91/−2, inside its CHARTER workspace); the owner's 3 evening commits correctly appear in no run row; #25's old blind spot closed by PR #39.
- scheduled_success_rate null = zero trigger rows yet; must be non-null in the 07-04 scorecard refresh after tonight's relay, else the scheduler reinstall didn't take (file then).

## Gardener (hygiene · ratchet)
- Retention: no-op verified — ops/reports/ holds only 2026-07-03.md (1 day old, cap 14 d); weekly/ untouched; zero files moved or deleted.
- Scorecard refreshed (ops/metrics/scorecard-2026-W27.json, committed below): runs 24→26, success 87→88%, cost $189.54→$210.38, manager row appears (1 run, $5.01), merged loop PRs 16, open filed/accepted/decisions 3/0/0.
- Ratchet: loc_product 3199→3287 (+88, fully attributed to owner commits 2c6bd77 + 8c705fe; under the 200 threshold), loc_tests 1885→1928 (+43), deps 0 (holds), collected 48 flat, pins 0 (known-failures.d/ empty) — Focus #3/#5 holding.
- Doc drift: none new — the owner's 3 evening commits left no dangling references (every named path exists; zero refs to retired known-failures.txt); the new doc-contract lint (classes 12–14 from 2c6bd77) ran green on its first night and now mechanically covers most of this grep surface. Open drift premises #35/#40/#41/#44 re-verified, all still valid, none re-filed.
- Ops hygiene: single worktree, 0 local loop branches, no stray ops/ files, manager workspace within CHARTER caps (notes 80/150, proposals ~1/100), state dir 21 MB (17 MB logs, within retention). 6 stale remote-tracking refs for merged loop PRs cleared this run via `git fetch --prune` (remote itself was already clean).

## Changes committed
- `loop: daily digest 2026-07-04` — ops/reports/2026-07-04.md, ops/metrics/2026-07-04.json, ops/metrics/scorecard-2026-W27.json (gardener refresh), ops/ledger/deploys.jsonl (3 owner-promote backfill rows 12:04/14:07/15:55Z). No code commits (gates green, nothing to revert). Non-commit maintenance: `git fetch --prune` (local metadata only).

## Proposals (ranked) / issues filed
1. **Filed: none.** Every finding tonight dedups to an open issue or is below the issue bar (standing order: when in doubt, do less and write it up): deploys.jsonl lag = #44 (backfilled, cooling ends 11:49Z); errored-run trace gap = #35; #49 = drill residue (Needs-you close, not a defect).
2. **Digest-only watches**: today's 04:33 journey must (a) produce a ledger row — else e2e-loop liveness FAIL, and (b) validate the 13-promote gap to 8c705fe — escalate on FAIL. Tomorrow's scorecard must show scheduled_success_rate non-null. 07-05 fixer oversubscription (~8 vs cap 3) — escalate to a 🙋 cap decision only if the backlog is still growing after 07-05. Verifier cost monotonic rise — flag only if it rises on a low-merge night.

## Skipped / degraded
- Deploy: nothing to promote — live == main at 8c705fe (owner promoted all three evening commits manually); auto lane still unexercised in production (not a defect, just untested).
- Queue grooming: nothing in scope — no loop-filed issue missing metadata, none >7 d without human signal, none obsolete (#49's close needs the owner, not grooming).
- Telemetry user-analytics: n/a (prod is the loop; 2 loop-generated journey identities excluded).
- History-gated alert rules (error rate >3× trailing median): skipped, <1 week of history; per-loop cost medians ACTIVE and clean.
- All 4 subagents returned STATUS: ok — no degraded stages.

## E2E user journey
> NOTIFY: deployed — e2e-user freshen promoted live 8c705fe → dfadb04 (gates 48/48 fresh, verify-live healthy); journey PASS at dfadb04

- **Agent**: fresh e2e-user session (no repo memory), driven from the live checkout only; fixture stub runner, auth path **native** (no credential needed).
- **Deploy-freshen**: drift.sh reported `drifted: live 8c705fe8e82f, origin/main dfadb04d22aa` (one commit — last night's digest). Tree clean + all 3 GATES run fresh and green → promoted per `deploy: auto`; `verify-live: healthy at dfadb04` (48/48, prompt-lint 0, shellcheck OK, floor drill bit). Rows in promotions.jsonl + ops/ledger/deploys.jsonl. This also closes yesterday's "validation gap" flag: the journey below validated the CURRENT engine.
- **Verdict: PASS** (6/6 stages) — nonce `e2e-faa1e388`, latency **64 s** (install → first-run green; 7-run trend ~57–64 s), duration ~400 s.
  - INSTALL: 3 ops dirs + 3 ledgers + config + DIRECTION present, fixture repo porcelain-clean.
  - FIRST-RUN: exit 0; runs.jsonl `"result":"success"`; preflight `"verdict":"run"`.
  - FLOORS: `--check-nogo` → `nogo_reverts=1 nogo_revert_failures=0`, exit 3, revert commit `5fd0d6c`, nonce grep 1→0 (file restored). PAUSE → row `"result":"paused"`, cost 0, log contains only `PAUSED:` — no agent invoked.
  - DOCS-HONESTY: every command copy-pasted from INTEGRATION.md steps 1/4 worked verbatim (the fixture's fake-HOME prefix is E2E_ENV_DESC-documented environment setup, not a doc deviation).
  - TEARDOWN: scratch removed, no leftover processes, dev repo porcelain-clean.
- **Issues filed**: none — no product failures, no doc drift.
- Evidence: `$ILOOP_STATE/evidence/e2e-user-2026-07-04/` (01-install … 07-teardown logs, env.txt with nonce).

## Fixer
Run 20260704-003128 — **no eligible bugs; 0 subagents dispatched** (as the funnel table predicted). All 4 open `bug` issues are unaccepted and still inside the 24 h cooling window (now = 2026-07-03T16:32Z):

- #35 (errored non-orchestrator runs leave no in-repo trace) — skipped: cooling until 2026-07-04T09:13:04Z; not `accepted`.
- #40 (dispatcher must pre-assign new-test ids) — skipped: cooling until 2026-07-04T09:52:10Z; not `accepted`.
- #41 (--check-nogo drill misses remote-tip scan) — skipped: cooling until 2026-07-04T09:53:28Z; not `accepted`.
- #44 (owner-manual promotes leave no in-repo deploy trace) — skipped: cooling until 2026-07-04T11:49:38Z; not `accepted`.

Develop lane: skipped — DIRECTION `develop_pipeline: off`.
All four thaw before the 07-05 05:03 fixer, which faces 4 eligible vs MAX_FIXES_PER_NIGHT=3 (oversubscription watch already logged by the orchestrator). No PRs opened, no NOTIFY lines (no whitelist category applies).

## PR verifier
Run 20260704-003408 — **queue empty; 0 subagents dispatched.** `gh pr list -R shenyuanv/general_improve_loop --label loop-pr --state open` → 0 PRs (no open PRs of any kind), matching the funnel table's prediction (fixer 05:03 was a no-op: all 4 open bugs unaccepted + cooling). No merges, no rejections, no CI_PENDING carryovers. `git pull --ff-only`: already up to date at f3619a9. Worktrees: 1 (main working tree only); tree clean. Expected next verification load: after the 07-05 05:03 fixer, when the 4 cooling bugs thaw (~3 PRs vs MAX_VERIFIES_PER_NIGHT=3).

## Re-run 03:33 (scheduled — run 20260704-033302)
Status: 🟢 GREEN — first **scheduled** relay run (Focus #2's Option-A meter now accruing); gates 48/48 · prompt-lint 0 · shellcheck clean at 27553bf; live current, no deploy needed; funnel 4 open / 0 orphans / 0 PRs / 0 decisions; 0 issues filed; all 4 subagents ok. The midnight digest's Needs-you is DONE: owner closed #49 at 16:22Z ("drill verified end-to-end … closing so a real failure files fresh").

### Guardian (safety)
- 🟡 CI watchdog: nightly-drills cron (18:17Z) has never fired as `schedule` — all 6 runs are manual dispatch; the first-ever cron window passed 83 min before this run with no show. Likely GitHub cron lag on a new schedule; escalate to an `action:operator` issue tomorrow if still zero scheduled runs.
- 🟢 Latest nightly-drills red (16:05Z) is the owner's planted drill; filer worked (#49 filed in 9 s), owner verified end-to-end and closed it — no open `component:ci` issue is the CORRECT state, not a filer failure. Plant step is dispatch-input-gated; scheduled runs unaffected.
- 🟢 Standing decisions hold: #5 GIT_REMOTE gone from both configs; #20 gh pinned to shenyuanv (alt inactive, 0 flips); #47 Option A premise intact — tonight's first scheduled run preflighted clean, no quota event; it seeds the previously-null `scheduled_success_rate`.
- 🟢 Recovery exercised, not assumed: 2 real rollbacks on 07-03 both restored green; latest journey PASS incl. no-go revert (nonce restored) and PAUSE floor; live = main = 27553bf.
- 🟡 State-dir durability: runs.jsonl / promotions.jsonl / evidence are machine-local, unbacked — machine loss erases the floor-bite evidence trail (repo survives on GitHub). Natural fix already queued as cooling #35/#44; no new issue.
- 🟢 Security: 0 auth failures, 0 floor violations (no-go/self-accept/breaker all 0), 0 errors in the last 11 runs; no credentials in telemetry.

### Operator (deploys · prod health · queue funnel)
**Deploy: ✅ current, no action.** Live checkout = 27553bf = main tip; drift.sh exit 0, verify-live --quick healthy. The 3-commit drift expected from the overnight ops commits never materialized — an untraced promote at 16:35:51Z (dfadb04→27553bf, seconds after the manual relay's pr-verifier finished) already freshened live. That promote has **no deploys.jsonl row — open #44's symptom, 5th consecutive recurrence**; backfill row appended this run (committed below). Deploy budget 0/1; no rollback; no ops/BLOCKED.

**Prod health:** preflight all-green (disk 44 GB, gh ok, agent auth ok). CI `ci` green at 8c705fe (later commits are ops-only, path-filtered). 24 h: 23 runs, $193.72, 21 ok / 2 error (trace gap = open #35), 0 timeouts, 0 no-go reverts. Both journeys in window PASS. 3 loop-generated journey identities excluded from aggregates.

**Queue funnel** (mechanical, `bin/funnel.sh` — exit 0):

| item | state | next actor | next action |
|---|---|---|---|
| #35 errored non-orchestrator runs leave no in-repo trace | cooling | fixer dispatcher | after 2026-07-04T09:13:04Z |
| #40 fixer dispatcher must pre-assign new-test ids | cooling | fixer dispatcher | after 2026-07-04T09:52:10Z |
| #41 --check-nogo drill doesn't exercise the remote-tip scan | cooling | fixer dispatcher | after 2026-07-04T09:53:28Z |
| #44 owner-manual promotes leave no in-repo deploy trace | cooling | fixer dispatcher | after 2026-07-04T11:49:38Z |

funnel: open_issues=4 loop_prs=0 orphans=0

All four thaw after today's 05:03 fixer (expected no-op, as the midnight funnel predicted) and before the 07-05 05:03 fixer: 4 eligible vs MAX_FIXES_PER_NIGHT=3 — oversubscription watch stands. Optional lever, not a blocker: `accepted`-labeling any of them before 05:03 waives its cooling and puts tonight's budget to work. #40/#41 are owner-authored (correctly not `loop-filed` — outside grooming scope). Open 🙋: none. New user reports: none. Open loop-pr PRs: none. Closed-in-24h traces: see the midnight Operator section; since then only #49 (owner-closed 16:22Z, drill residue).

### Analysis (E2E trends)
- Journeys 14 d: ▁▆▆ PARTIAL→PASS→PASS (2 PASS / 1 PARTIAL / 0 FAIL); the only stage ever to fail (DOCS_HONESTY) is confirmed fixed live (PR #17), no stage failed twice.
- Latency install→first-green: 58·57·64 s — latest 1.10× median, no regression. Watch: total journey duration_s 175→292→400 (2.3× over 3 rows) while the ruled metric stays flat — becomes an efficiency finding if it doubles again with no new stages.
- Liveness: journey 0.13 d ago ✓ and it validated the CURRENT engine; tester 0 rows on plan (first sweep 07-05); all 4 bugs inside cooling so no-PR is correct; 0 open PRs, nothing stuck.
- Drift: none — auth_path native ×3, infra_flags [] ×3, 0 INFRA_FAIL rows.
- Cost (Focus #4): latest/median7 = orch 1.38× · fixer 0.14× · verifier 0.17× · e2e 0.88× — verifier's predicted fall on the empty queue happened ($8.77→$1.11); max run in file $16.69, nothing >$25; orchestrator §3.5 independent check concurs (all ok). Diff accounting: 4/4 newest rows byte-exact vs git (dfadb04 +216/−11 · ebbfd22 +16 · f3619a9 +11 · 27553bf +3).

### Gardener (hygiene · ratchet)
- Retention: no dailies older than 14 d (oldest 2026-07-03); weekly rollup untouched, 0 files deleted.
- Ratchet: product 3287 / tests 1928 / deps 0 / collected 48 — identical to the midnight metrics; the 3 ops-only commits since added 0 product LOC. Pins 0. No dependency manifest appeared.
- Scorecard refreshed (ops/metrics/scorecard-2026-W27.json, committed below): window runs 26→30, success 88%→90%, cost $210.38→$232.58, journeys 2→3 rows, open loop-filed 3→2, bugs 5→4; scheduled_success_rate still null — this run's row lands only after it closes, as expected; tomorrow's refresh must show it non-null (standing watch from the midnight digest).
- Doc drift: clean sweep — 0 retired-name hits, all doc-referenced paths exist, 0 unanchored config-var references; matches prompt-lint 0/0.
- Ops hygiene: ops/ 224 K all contract-defined; single worktree; manager workspace 80/150 + 7/100 under caps; state dir 23 M (19 M logs from the dense owner-driven day — run-loop retention caps apply, watching, no action).

### Changes committed
- `loop: scheduled re-run 2026-07-04 03:33` — ops/reports/2026-07-04.md (this section), ops/metrics/2026-07-04.json (scheduled_rerun block), ops/metrics/scorecard-2026-W27.json (gardener refresh), ops/ledger/deploys.jsonl (1 backfill row: 16:35:51Z dfadb04→27553bf, #44 evidence). No code commits; nothing to revert.

### Proposals (ranked) / issues filed
1. **Filed: none.** Every finding dedups to an open issue (#44: 5th untraced promote, backfilled; #35: error-trace gap) or is below the issue bar.
2. **Watches handed to tomorrow's guardian/orchestrator:** (a) nightly-drills must show ≥1 `event=schedule` run by tomorrow's guardian pass — else file `action:operator` `component:ci`; (b) 07-05 scorecard must show scheduled_success_rate non-null — else the scheduler reinstall didn't take; (c) today's 04:33 journey must produce a ledger row; (d) 07-05 fixer oversubscription (4 vs cap 3); (e) journey duration_s growth (2.3×/3 rows).

### Skipped / degraded
- Deploy: no-op — live already current (untraced 16:35:51Z promote; auto lane still unexercised in production).
- Queue grooming: nothing in scope — loop-filed issues (#35 #44) fully formed; #40/#41 human-filed; none >7 d without signal; none obsolete.
- NOTIFY: none — no whitelist category occurred this run (no deploy, failure, rollback, incident, pending-ack, new user, or decision).
- All 4 subagents STATUS: ok — no degraded stages.

## E2E user journey   (scheduled run 20260704-043301, 04:33)
> NOTIFY: rollback — deploy-freshen promoted c292a92 then rolled back to 27553bf: verify-live red from env-leak bug #50 (main itself green 48/48); live re-verified healthy
> NOTIFY: failure — #50: wrapper leaks LOOP_TIMEOUT_S into agent env; every scheduled loop except orchestrator sees red gates (t26) — fixer 05:03 and pr-verifier 06:03 TONIGHT will hit it (fix = tests/lib.sh sanitize)

- **Verdict: PASS** — all 6 stages green (INSTALL, FIRST-RUN, FLOORS-NOGO, FLOORS-PAUSE, DOCS-HONESTY, TEARDOWN). Ledger: journeys.jsonl ts above, run_id 20260704-043301.
- Agent: fresh e2e-user subagent (no repo memory), driven from the live checkout only. Auth path: native (fixture stub runner, no credential). Nonce `e2e-1b70e8bd-20260704-0433` planted → reverted → grep 0.
- Latency install→first-green: **85 s** (trend 58·57·64·85, 1.3× median) — ~58 s of it is attempt 1: the verbatim doc command hit a silent preflight abort (rc=1, zero output) before the fake-HOME retry; the diagnosis dead-end is filed **#51** (`component:docs`). Not a doc lie for a real adopter (fake-HOME is the E2E_ENV_DESC sandbox artifact, precedent run 20260704-002329), so DOCS-HONESTY stays PASS.
- Floors bit: `--check-nogo` exit 3, revert 95a09a9, DIRECTION restored (nonce grep 1→0); PAUSE row `result:paused` cost 0, no agent invoked.
- Deploy-freshen (deploy: auto): drift 27553bf→c292a92 (ops-files-only); local gate run came back RED (t26 deadline drift −599 s) — root-caused live to ambient `LOOP_TIMEOUT_S=5400` leaking from this wrapped run into the nested suite (`run-loop.sh:45`), mechanically proven (`env -u LOOP_TIMEOUT_S` → green). Reused today's 03:33 green metrics (48/48) per the operator rule and promoted; **verify-live failed on the same env leak → one-attempt rule: rolled back to 27553bf, `verify-live --quick` healthy**. Rows in deploys.jsonl. Filed **#50** (`bug` + `action:loop` + `component:engine`) with repro + verify; flagged that trigger:manual runs escape the leak (env unset) — why tonight's earlier manual fixer/verifier passes were green.
- Issues filed: #50 (env leak, blocked tonight's deploy), #51 (silent preflight abort undocumented). Digest note (below issue bar): `selfhost/fixture.sh` prints only the config path — printing the ready-to-paste `HOME=<dest>/home bash …/run-loop.sh orchestrator <cfg>` command would remove the fixture's first-run trap entirely.
- Evidence: `$ILOOP_STATE/evidence/e2e-user-2026-07-04/run-20260704-043301/` (per-stage transcripts, timings, docs-honesty table). Teardown: scratch removed, dev repo porcelain 0.
- Watch (c) from the 03:33 orchestrator (this journey must produce a ledger row): satisfied.

## Fixer   (scheduled run 20260704-050300, 05:03)
Run 20260704-050300 — **no eligible bugs; 0 subagents dispatched** (the funnel table's predicted no-op, now with 2 more coolers from the 04:33 journey). All 6 open `bug` issues are unaccepted (`accepted` bypasses cooling; none carries it) and inside the 24 h cooling window (now = 2026-07-03T21:04Z):

- #35 (errored non-orchestrator runs leave no in-repo trace) — skipped: cooling until 2026-07-04T09:13:04Z; not `accepted`.
- #40 (dispatcher must pre-assign new-test ids) — skipped: cooling until 2026-07-04T09:52:10Z; not `accepted`.
- #41 (--check-nogo drill misses remote-tip scan) — skipped: cooling until 2026-07-04T09:53:28Z; not `accepted`.
- #44 (owner-manual promotes leave no in-repo deploy trace) — skipped: cooling until 2026-07-04T11:49:38Z; not `accepted`.
- #50 (wrapper leaks LOOP_TIMEOUT_S into agent env; blocked tonight's deploy) — skipped: cooling until 2026-07-04T20:47:56Z; not `accepted`. Filed 16 min before this run by the 04:33 journey — a brand-new finder-filed bug never flows to a merge the same night.
- #51 (silent preflight abort undocumented in INTEGRATION.md) — skipped: cooling until 2026-07-04T20:48:18Z; not `accepted`.

Develop lane: skipped — DIRECTION `develop_pipeline: off`.

Notes for the 07-05 05:03 fixer (≈2026-07-04T21:03Z): all 6 thaw by then (#50/#51 barely — 20:47/20:48Z), so it faces **6 eligible vs MAX_FIXES_PER_NIGHT=3** — the standing oversubscription watch worsens from 4 to 6. Suggested triage order if the owner wants to steer with `accepted` labels: #50 first (unfixed, its LOOP_TIMEOUT_S leak makes every *scheduled* fixer/verifier subagent see a spuriously red `GATES[0]` — it degrades the very lane that must fix it), then the cheap doc fix #51. This run itself dispatched no gate-running subagents, so #50's leak had nothing to bite tonight.

Housekeeping verified: 0 open PRs of any kind, no `loop/fix-GH<n>` branches, single worktree, main clean at a04f781 == origin/main. No PRs opened ⇒ no NOTIFY lines (none of the whitelist categories occurred; #50's failure NOTIFY already went out with the 04:33 journey — not duplicated).

## PR verifier   (scheduled run 20260704-060302, 06:03)
Run 20260704-060302 — **queue empty; 0 subagents dispatched.** `gh pr list -R shenyuanv/general_improve_loop --label loop-pr --state open` → 0 PRs (no open PRs of any kind), exactly as the funnel table and the 05:03 fixer's no-op predicted (all 6 open bugs unaccepted + cooling ⇒ no fix PRs to verify). No merges, no rejections, no CI_PENDING carryovers; no verdicts to execute.

- Corroboration for **#50** (live, free): this scheduled run's environment carries the leaked `LOOP_TIMEOUT_S=3600` exactly as the 04:33 journey predicted for the 06:03 verifier. It had nothing to bite ONLY because zero verify subagents ran — the first scheduled verifier that actually reruns `GATES[0]` in a subagent will see the spurious t26 red until #50 lands (thaws 20:47Z, eligible for the 07-05 05:03 fixer). No new issue filed: dedups cleanly onto open #50.
- `git pull --ff-only`: already up to date at ac4f098 == origin/main. Worktrees: 1 (main working tree only); no loop branches; tree clean.
- Expected next verification load: 07-05 06:03, after the 07-05 05:03 fixer works the 6-thawed / cap-3 backlog (~3 PRs vs MAX_VERIFIES_PER_NIGHT=3). If the owner `accepted`-labels #50 today it bypasses cooling — but its fix would still arrive via a manual fixer run, not this verifier.

No NOTIFY lines: no whitelist category occurred (no merge ⇒ no merged-PR notify; no failure/deploy/rollback/incident/decision).

## Manager (strategy)   (weekly run 20260704-083300, Sat 08:33)
> NOTIFY: decision needed #52 — 07-05 fixer triage steer (6 thawed bugs vs cap 3; #50 degrades the scheduled lane — accept #50 first / cap-raise / do nothing / fix interactively)

**Convergence verdict (scorecard-2026-W27 @ 07-03 19:38Z + runs.jsonl through 06:03Z).** Focus #1 GREEN and newly *proven*: 14-day window 0 no-go reverts / 0 self-accept strips / 0 breaker trips, and the deploy floor bit unattended for the first time — the 04:33 promote 27553bf→c292a92 failed verify-live, one-attempt rollback restored green (the auto lane's first real failure was caught, not shipped). Focus #2 GREEN on day one of its decided basis (#47-A): first scheduled relay 4/4 success (orchestrator·e2e-user·fixer·pr-verifier), 0 orphans, diff accounting reconciles; headline 90% is quota noise by standing decision. Focus #3 is the one item trending WRONG: pins 0 and collected 48 hold, but merges = 0 in ~30 h while open bugs grew 4→6 (all unaccepted/cooling), and #50 makes every *scheduled* gate-running subagent see spurious red — the lane that must fix it is the lane it degrades; tomorrow: 6 eligible vs MAX_FIXES_PER_NIGHT=3. Focus #4 GREEN: all loops ≤1.38× their 7-run median; the first all-scheduled day cost $19.99 total — well under the $60/day watch line. Focus #5 GREEN: doc-contract lint green on night one, deps 0, one new docs gap already queued (#51). **Single biggest risk: #50's ordering** — resolved by a one-tap owner steer, hence the only proposal this week.

- Proposal: 🙋 **#52** — triage steer for the oversubscribed 07-05 fixer (A: accept #50 first, recommended · B: +one-night cap raise · C: nothing · D: fix interactively today). Decision window: before 2026-07-04T21:03Z.
- Past proposals both landed: **#47** → owner chose Option A, enacted 07-03 (DIRECTION reword + `--scheduled` plists; first meter reading 4/4 above). **#48** → enacted 07-03 as the doc-contract lint (2c6bd77), green on night one. Acceptance 2/2.
- Design brief: none this week — the highest-value engineering items are already queued as bugs (#50, #44, #35); nothing design-grade lacks a queue expression yet.
- Parsimony: none qualifying — youngest mechanisms (develop lane, CI finders, manager itself) are 1 day old; the auto-deploy lane justified itself last night (caught bad promote). Revisit with a steady-state week.
- Audit tickler: not due — newest HARNESS_AUDIT entry is 2026-07-03 (1 day old, bar is >14 d).

## Fixer   (run 20260704-100354, 10:03 — off-schedule, owner steer per #52)
Run 20260704-100354 — **3 dispatched, 3 PRs opened** (cap MAX_FIXES_PER_NIGHT=3). The owner enacted #52's steer between 06:03 and now: `accepted` on #35/#40/#41/#44/#50 (bypasses cooling) + a binding comment on #50 ("rank this FIRST … fix scope: wrapper must not leak LOOP_TIMEOUT_S into the agent environment").

- **#50** (LOOP_TIMEOUT_S leak; owner-ranked first) — **pr-opened**: https://github.com/shenyuanv/general_improve_loop/pull/54. Repro before: `LOOP_TIMEOUT_S=5400 bash tests/run.sh t26` → "deadline drift -599 s"; after: passes, suite 49/49 green under both `LOOP_TIMEOUT_S=3600` and clean env. Fix per the binding scope: `bin/run-loop.sh` unsets LOOP_TIMEOUT_S after capturing TIMEOUT_S, plus tests/lib.sh env scrub (per-test knobs like `LOOP_TIMEOUT_S=2` still win) + new t49-timeout-env-scrub.sh.
- **#40** (dispatcher pre-assigns new-test ids) — **pr-opened**: https://github.com/shenyuanv/general_improve_loop/pull/53. Prompt-only, 2 files +9/−2 (fixer.md §2 assignment rule + fix-one-issue.md step 3); Verify greps fail-before/pass-after, prompt-lint 0, suite 48/48 in its worktree. This run already practiced the rule it codifies: subagents were pre-assigned t49/t50/t51 (t51 unused).
- **#35** (errored non-orchestrator runs leave no in-repo trace) — **pr-opened**: https://github.com/shenyuanv/general_improve_loop/pull/55. Sandbox repro before: rc=1 e2e-user run left runs.jsonl error row but no digest line; after: digest gains `> NOTIFY: failure — …` line. `bin/run-loop.sh` +10 (post-run §6 only, no overlap with #54's hunk), contracts/digest-format.md sync +5, new t50-failed-run-digest-trace.sh; suite 49/49 (collected UP).

Skipped (eligible, over cap — remain `accepted` for the next fixer): #41 (--check-nogo remote-tip drill), #44 (deploys.jsonl reconcile). Skipped (cooling): #51 — unaccepted, cools until 2026-07-04T20:48Z. Develop lane: skipped — DIRECTION `develop_pipeline: off`.

Note for the 06:03→next pr-verifier: #54 and #55 both touch `bin/run-loop.sh` in disjoint sections (top-of-file env capture vs post-run §6); after the first squash-merge the second PR needs a rebase-free re-verify against updated main — if it stale-PASSes into a conflict, the close-and-re-arm path applies. All three fix branches carry `loop-pr`; worktrees verified removed (single main worktree, tree clean at 502db72).

> NOTIFY: loop PR opened: https://github.com/shenyuanv/general_improve_loop/pull/54 (#50)
> NOTIFY: loop PR opened: https://github.com/shenyuanv/general_improve_loop/pull/53 (#40)
> NOTIFY: loop PR opened: https://github.com/shenyuanv/general_improve_loop/pull/55 (#35)

## PR verifier   (run 20260704-101525, 10:15 — off-schedule, follows the 10:03 owner-steered fixer)
Run 20260704-101525 — **3 verified, 3 merged, 0 rejected** (queue = cap: MAX_VERIFIES_PER_NIGHT=3). One fresh subagent per PR, zero shared context; all three returned PASS on all 7 checks; verdicts executed oldest-first.

> NOTIFY: merged loop PR #53 — fixes #40
> NOTIFY: merged loop PR #54 — fixes #50
> NOTIFY: merged loop PR #55 — fixes #35

- **PR #53** (fixer pre-assigns new-test ids, fixes #40) — **PASS ⇒ merged** (squash dacbefc). Bug real at base 502db72 (assignment-rule grep exits 1 on both prompt files); PR head makes issue's full Verify chain pass; gates 48/48 + prompt-lint 0 + shellcheck clean on base AND head; scope 2 files +9/−2 (prompt-only, FIX lane, 0 NOGO); CI green; body's before/after and diff-stat claims all reproduced.
- **PR #54** (scrub LOOP_TIMEOUT_S from agent env, fixes #50) — **PASS ⇒ merged** (squash 1935ff3). Repro verbatim at base: `LOOP_TIMEOUT_S=5400 bash tests/run.sh t26` → "deadline drift -599 s"; at head t26 green under 5400, suite 49/49 under LOOP_TIMEOUT_S=3600 AND clean env; new t49 fails-before/passes-after with the exact claimed assert; owner's binding scope comment ("wrapper must not leak") implemented exactly; 52+/1− across run-loop.sh, tests/lib.sh, t49; collected UP 48→49; CI green. Mergeability re-checked CLEAN after #53's merge — no stale-PASS.
- **PR #55** (errored runs leave in-repo digest trace, fixes #35) — **PASS ⇒ merged** (squash d01549c). Bug real at base (rc=1 e2e-user run: runs.jsonl error row, no digest); at head t50 green + sandbox failed-run writes the `> NOTIFY: failure —` digest line, and the verifier's adversarial extra confirmed the trace line is NOT re-fanned as a duplicate notification; contracts/digest-format.md sync respects the two-layer rule; 43+ lines, 0 NOGO; collected 48→49; CI green. Mergeability re-checked CLEAN after #53+#54 (disjoint run-loop.sh hunks, as the fixer predicted) — the close-and-re-arm path was armed but not needed.
- Housekeeping: `git pull --ff-only` → main at d01549c (= origin); issues #40/#50/#35 auto-closed via `Fixes #n`; remote fix branches deleted (stale tracking refs pruned via `git fetch --prune`); 3 fully-merged local `loop/fix-GH*` branches left for the wrapper's stale-branch prune floor; single worktree, tree clean. Verify subagents wrote nothing; each removed its own worktree.
- Fail-count watch: none of tonight's PRs has any prior FAIL — no close recommendations.
- Corroboration retired: the 06:03 verifier's standing #50 watch (scheduled subagents see spurious t26 red) is now moot — #54 is merged; the next scheduled gate-running subagent should be green under the leaked var. Expected next load: 07-05 06:03, after the 07-05 fixer works the remaining backlog (#41, #44 accepted-over-cap; #51 thaws 20:48Z).
